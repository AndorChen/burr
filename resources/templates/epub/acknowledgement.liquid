{% extends 'layout' %}

{% block title %}{{ item.title }}{% endblock %}

{% block body %}
<div class="item acknowledgement">
  <h1 id="{{ item.id }}">{{ item.title }}</h1>
  {{ item.content }}
</div>
{% endblock %}
